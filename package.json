{
  "name": "knopvvs",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "api:dev": "npm run start:dev -w api",
    "api:test": "npm run test -w api",
    "web:dev": "npm run dev -w web"
  }
}
